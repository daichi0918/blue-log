import { Meta, StoryObj } from "@storybook/react";

import { ScrollTopIcon } from ".";

export default {
  title: "atoms/ScrollTopIcon",
  component: ScrollTopIcon,
  tags: ["autodocs"],
} as Meta;

type Story = StoryObj<typeof ScrollTopIcon>;

export const DefaultScrollTopIcon: Story = {};
